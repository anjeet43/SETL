"use client";
import { useMemo, useState } from "react";
import { roomCategories, type Product } from "@/lib/types";
function RoomPick({ product }: { product: Product }) { return <article className="flex min-w-0 items-center gap-3 border border-white/20 bg-white/5 p-2"><div className="h-14 w-14 shrink-0 overflow-hidden bg-clay">{product.image_url ? <img src={product.image_url} alt="" className="h-full w-full object-cover"/> : <div className="grain h-full"/>}</div><div className="min-w-0 flex-1"><p className="truncate text-xs font-bold">{product.name}</p><p className="mt-1 text-xs text-white/70">₹{product.price.toLocaleString("en-IN")}</p></div><form action="/api/cart" method="post"><input type="hidden" name="productId" value={product.id}/><button disabled={product.stock_quantity < 1} className="border border-lemon px-2 py-1 text-xs font-bold text-lemon disabled:opacity-40">+</button></form></article> }
export function RoomBuilder({ products }: { products: Product[] }) { const [active, setActive] = useState<string | null>(null); const visible = useMemo(() => active ? products.filter((p) => p.category_slug === active) : products, [active, products]); return <section id="room" className="border-y border-black/10 bg-moss px-5 py-12 text-white md:py-16"><div className="mx-auto max-w-6xl"><div className="flex flex-wrap items-end justify-between gap-6"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-lemon">The Setl signature</p><h2 className="display mt-2 text-5xl md:text-6xl">Build your room.</h2><p className="mt-3 max-w-md text-sm leading-6 text-white/70">Pick what you’re setting up. Your setup becomes more complete with each category.</p></div><div className="flex h-28 w-28 items-center justify-center rounded-full border-[7px] border-lemon text-center"><span><b className="block text-3xl">0%</b><small className="text-[10px] uppercase tracking-wide">ready</small></span></div></div><div className="mt-8 grid grid-cols-2 gap-px overflow-hidden border border-white/20 sm:grid-cols-3 lg:grid-cols-6">{roomCategories.map((category) => <button onClick={() => setActive(active === category.slug ? null : category.slug)} key={category.slug} className={`p-5 text-left transition ${active === category.slug ? "bg-lemon text-ink" : "bg-white/5 hover:bg-white/10"}`}><span className="text-xl">{category.icon}</span><span className="mt-7 block text-sm font-bold">{category.name}</span><span className="mt-1 block text-xs opacity-65">{products.filter(p => p.category_slug === category.slug).length || "No"} picks</span></button>)}</div>{active && <div className="mt-6 border-t border-white/20 pt-4"><div className="mb-3 flex items-center justify-between"><p className="text-sm font-bold">{roomCategories.find(c => c.slug === active)?.name} picks</p><button className="text-xs underline" onClick={() => setActive(null)}>Close</button></div><div className="grid gap-2 sm:grid-cols-2 lg:grid-cols-3">{visible.map(p => <RoomPick product={p} key={p.id} />)}</div></div>}</div></section>; }
