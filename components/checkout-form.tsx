"use client";
import { useState } from "react";
declare global { interface Window { Razorpay?: new (options: Record<string, unknown>) => { open: () => void } } }
function loadRazorpay() { return new Promise<boolean>((resolve) => { if (window.Razorpay) return resolve(true); const script = document.createElement("script"); script.src = "https://checkout.razorpay.com/v1/checkout.js"; script.onload = () => resolve(true); script.onerror = () => resolve(false); document.body.appendChild(script); }); }
export function CheckoutForm() { const [error, setError] = useState(""); const [loading, setLoading] = useState(false); const [complete, setComplete] = useState(false); async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setLoading(true); setError(""); try { const form = new FormData(event.currentTarget); const response = await fetch("/api/checkout", { method: "POST", body: form }); const data = await response.json(); if (!response.ok) throw new Error(data.error || "Could not start checkout."); if (!await loadRazorpay() || !window.Razorpay) throw new Error("Secure payment window could not load. Please check your connection."); const payment = new window.Razorpay({ key: data.key, amount: data.amount, currency: data.currency, name: "Setl", description: "Your campus setup", order_id: data.razorpayOrderId, theme: { color: "#184f42" }, handler: () => setComplete(true), modal: { ondismiss: () => setLoading(false) } }); payment.open(); } catch (err) { setError(err instanceof Error ? err.message : "Could not start payment."); } finally { setLoading(false); } } if (complete) return <div className="mt-10 border border-moss bg-white p-6"><p className="text-xs font-bold uppercase tracking-[.18em] text-moss">Payment submitted</p><h2 className="display mt-3 text-4xl">We’re confirming it now.</h2><p className="mt-3 text-sm leading-6 text-black/65">Your order is marked paid only when Razorpay’s signed confirmation reaches Setl. Please don’t submit payment again.</p></div>; return <form className="mt-10 grid gap-4 border border-black bg-white p-5" onSubmit={submit}><label className="text-xs font-bold">Your name<input required name="name" className="mt-1 w-full border border-black/20 p-3 text-sm"/></label><label className="text-xs font-bold">Phone<input required name="phone" inputMode="tel" className="mt-1 w-full border border-black/20 p-3 text-sm"/></label><div className="grid grid-cols-2 gap-3"><label className="text-xs font-bold">Hostel<select required name="hostel" className="mt-1 w-full border border-black/20 bg-white p-3 text-sm"><option value="">Select</option><option>Hostel 1</option><option>Hostel 2</option><option>Other</option></select></label><label className="text-xs font-bold">Room<input required name="room" className="mt-1 w-full border border-black/20 p-3 text-sm"/></label></div><label className="text-xs font-bold">Delivery note (optional)<textarea name="note" className="mt-1 w-full border border-black/20 p-3 text-sm"/></label><button disabled={loading} className="bg-ink p-4 text-sm font-bold text-white disabled:opacity-50">{loading ? "Preparing secure payment…" : "Continue to secure payment"}</button>{error && <p className="text-sm text-red-700">{error}</p>}<p className="text-xs leading-5 text-black/50">Prices and stock are rechecked securely before payment. Setl never treats the payment window as confirmation.</p></form>; }
