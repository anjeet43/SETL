import { NextRequest, NextResponse } from "next/server";
export async function POST(request: NextRequest) { const form = await request.formData(); const id = String(form.get("productId") || ""); const action = String(form.get("action") || "add"); if (!id) return NextResponse.redirect(new URL("/products", request.url)); let cart: Record<string, number> = {}; try { const previous = request.cookies.get("setl_cart")?.value; cart = previous ? JSON.parse(decodeURIComponent(previous)) : {}; } catch {} if (action === "remove") delete cart[id]; else if (action === "decrement") { const next = (cart[id] || 0) - 1; if (next > 0) cart[id] = next; else delete cart[id]; } else { const next = (cart[id] || 0) + 1; delete cart[id]; cart[id] = next; } const referer = request.headers.get("referer"); const returnTo = referer && new URL(referer).origin === new URL(request.url).origin ? referer : new URL("/products", request.url).toString(); const response = NextResponse.redirect(returnTo); response.cookies.set("setl_cart", JSON.stringify(cart), { httpOnly: true, sameSite: "lax", maxAge: 60 * 60 * 24 * 14, path: "/" }); return response; }
